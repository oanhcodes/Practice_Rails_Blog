<% if current_user %>
  <h3>Post a Comment</h3>

  <%= form_for [ @article, @comment ] do |f| %>
    <p>
      <%= f.label "Your Comment" %><br/>
      <%= f.text_area :body %>
    </p>
    <p>
      <%= f.submit 'Submit' %>
    </p>
  <% end %>
<% end %>